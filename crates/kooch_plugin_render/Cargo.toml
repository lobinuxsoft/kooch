[package]
name = "kooch_plugin_render"
description = "GPU passes for Kooch plugins: the wgpu half of the plugin API"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

# 🔴 Linked only by a plugin that draws. `kooch_plugin_api` keeps its promise of costing nothing to
# link, and wgpu is behind this door instead (#392).
[dependencies]
kooch_plugin_api.workspace = true
wgpu.workspace = true
