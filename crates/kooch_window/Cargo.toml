[package]
name = "kooch_window"
description = "Windowing system using winit"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
kooch_core.workspace = true
winit.workspace = true
# Decodes the embedded window icon. Already in the workspace with
# default-features off (png + jpeg only), so this adds a name to one
# Cargo.toml rather than a codec tree to the build.
image.workspace = true
raw-window-handle.workspace = true
tracing.workspace = true
