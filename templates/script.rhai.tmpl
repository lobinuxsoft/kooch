// {{name}} — Rhai behaviour script.
//
// Generated from `templates/script.rhai.tmpl`. Edit that file to change
// the scaffold for every new Rhai script.
//
// Scripts run through the engine's scripting backend (RhaiBackend):
// `compile(source)` returns a handle, then `run(handle)` evaluates the
// whole script, or `call(handle, "{{name}}", args)` invokes a function.

fn {{name}}() {
    // Your behaviour here. Return a value, or mutate host-exposed state.
    print("{{name}} ran");
}
